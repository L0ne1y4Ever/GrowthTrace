@startuml
start
:用户填写自我描述;
:系统校验文本完整性;
if (是否请求 AI 画像草稿?) then (是)
  :后端调用 AI 服务;
  :生成画像草稿;
else (否)
  :用户手工填写画像字段;
endif
:展示待确认画像内容;
if (用户确认?) then (是)
  :保存正式成长画像;
  :计算画像完整度;
else (否)
  :继续编辑或暂存草稿;
endif
stop
@enduml
