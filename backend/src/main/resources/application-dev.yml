spring:
  config:
    import: optional:classpath:application-dev.local.yml
  datasource:
    url: jdbc:mysql://localhost:3306/growthtrace?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    root: INFO
    com.growthtrace: DEBUG
    org.springframework.security: INFO
